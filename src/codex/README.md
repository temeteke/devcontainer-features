# Codex Feature

Installs OpenAI Codex CLI via npm and persists `~/.codex`.

## Options

- `install`: default `true`
- `persist`: default `true`
- `version`: default `latest`

## Persisted paths

- `~/.codex`
