# OpenCode Feature

Installs OpenCode and persists OpenCode config/data directories.

## Options

- `install`: default `true`
- `persist`: default `true`
- `version`: npm package version, default `latest`

## Persisted paths

- `~/.config/opencode`
- `~/.local/share/opencode`
