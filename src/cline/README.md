# Cline Feature

Installs the Cline VS Code extension and persists its customization directories.

## Options

- `install`: kept for consistency. The VS Code extension is added via `customizations.vscode.extensions`.
- `persist`: default `true`

## Persisted paths

- `~/Documents/Cline`
- `~/.cline`
