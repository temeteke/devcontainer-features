{
  "id": "agents",
  "version": "0.1.0",
  "name": "Shared Agents State",
  "description": "Persists ~/.agents as shared state for agent tools and skills.",
  "options": {
    "persist": {
      "type": "boolean",
      "default": true,
      "description": "Persist shared agent state by linking ~/.agents to the host home directory."
    }
  },
  "mounts": [
    {
      "type": "bind",
      "source": "${localEnv:HOME}/.agents",
      "target": "/mnt/devcontainer-features/agents"
    }
  ],
  "postCreateCommand": "/usr/local/share/devcontainer-features/agents/setup-persistence.sh"
}
