# Shared Agents Directory Feature

Persists `~/.agents` so tools can share installed skills and related agent state across Dev
Container rebuilds.

This feature is intended to be used directly or as a dependency from other agent/tool features
such as OpenCode.

## Options

- `persist`: default `true`

## Persisted paths

- `~/.agents`
