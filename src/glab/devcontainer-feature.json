{
  "id": "glab",
  "version": "0.1.0",
  "name": "glab",
  "description": "Installs the GitLab CLI (glab) and persists its configuration directory.",
  "options": {
    "install": {
      "type": "boolean",
      "default": true,
      "description": "Install glab."
    },
    "persist": {
      "type": "boolean",
      "default": true,
      "description": "Persist glab configuration by linking ~/.config/glab-cli to the host home directory."
    },
    "version": {
      "type": "string",
      "default": "latest",
      "description": "glab version to install (e.g., 1.53.0). Use 'latest' for the most recent release."
    }
  },
  "mounts": [
    {
      "type": "bind",
      "source": "${localEnv:HOME}/.config/glab-cli",
      "target": "/mnt/devcontainer-features/glab/config"
    }
  ],
  "postCreateCommand": "/usr/local/share/devcontainer-features/glab/setup-persistence.sh"
}
